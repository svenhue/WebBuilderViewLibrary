import { BaseView } from "alphautils";
import { type IToolbarViewConfiguration } from "./ToolbarViewConfiguration.js";

export class ToolbarViewElement extends BaseView{
    constructor(config: IToolbarViewConfiguration ){
        super(config)
    }

    
}